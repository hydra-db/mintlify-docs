---
title: "Retry Delivery"
description: "Re-attempt a previously failed webhook delivery."
openapi: "api-reference/v2/openapi.json POST /webhooks/indexing/deliveries/{delivery_id}/retry"
---

import { Field } from "/snippets/field.jsx";

Re-sends a specific delivery to your registered `url`. Use it after fixing a receiver outage
to replay a `failed` delivery found via [List Deliveries](/api-reference/v2/endpoint/list-deliveries).

<RequestExample>

```python Python SDK
resp = client.webhooks.retry_delivery(delivery_id="dlv_1234")
```

```typescript TypeScript SDK
const resp = await client.webhooks.retryDelivery({ deliveryId: "dlv_1234" });
```

```bash cURL
curl -X POST 'https://api.hydradb.com/webhooks/indexing/deliveries/dlv_1234/retry' \
  -H "Authorization: Bearer <your_api_key>" \
  -H "API-Version: 2"
```

</RequestExample>

<Tip>
  **Related Resources**

  - **Find failures:** [List Deliveries](/api-reference/v2/endpoint/list-deliveries)
  - **Inspect one:** [Get Delivery](/api-reference/v2/endpoint/get-delivery)
</Tip>
