<Warning>
**HydraDB v1 is being phased out.** Support will be dropped soon and these pages are no longer updated — for new work, use the [v2 documentation](/get-started/v2/introduction) (or switch versions in the sidebar).

Heads-up: `pip install hydradb-sdk` installs the current **2.x** SDK — no 1.x package was ever published — so the v1 code samples on these pages may not run against a fresh install.
</Warning>
