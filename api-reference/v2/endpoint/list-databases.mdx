---
title: "List Databases"
description: "List all databases created. "
openapi: "api-reference/v2/openapi.json GET /databases"
---

The response separates active or provisioning databases (in `data.databases`) from databases whose provisioning failed (in `data.failed_databases`). Use [Database Status](https://docs.hydradb.com/api-reference/v2/endpoint/database-status) to confirm readiness before ingestion.

<RequestExample>

```python Python SDK
response = client.tenants.list()
```

```typescript TypeScript SDK
const response = await client.tenants.list();
```

```bash cURL
curl -X GET 'https://api.hydradb.com/tenants' \
  -H "Authorization: Bearer <your_api_key>" \
  -H "API-Version: 2"
```

</RequestExample>

<ResponseExample>

```json Success
{
  "success": true,
  "data": {
    "databases": ["acme_corp", "my_first_tenant"],
    "failed_databases": null,
    "message": "Successfully retrieved tenant IDs"
  },
  "error": null,
  "meta": {
    "request_id": "9d13aef4-02f4-4e73-8c62-4c2601d04f9d",
    "latency_ms": 12.3
  }
}
```

```json Provisioning issue
{
  "success": true,
  "data": {
    "databases": ["acme_corp"],
    "failed_databases": [
      {
        "database": "staging_import",
        "error": "Provisioning failed. Re-create the tenant to retry."
      }
    ],
    "message": "Successfully retrieved tenant IDs"
  },
  "error": null,
  "meta": {
    "request_id": "9d13aef4-02f4-4e73-8c62-4c2601d04f9d",
    "latency_ms": 12.3
  }
}
```

```json Failure
{
  "success": false,
  "data": null,
  "error": {
    "code": "UNAUTHORIZED",
    "message": "Missing, expired, or invalid API key"
  },
  "meta": {
    "request_id": "9d13aef4-02f4-4e73-8c62-4c2601d04f9d",
    "latency_ms": 4.8
  }
}
```

</ResponseExample>

## Retry notes

- If provisioning failed for a database, `data.failed_databases` contains diagnostic entries as shown in the **Provisioning issue** tab.
- **Retry failed databases:** If a database appears in `data.failed_databases`, re-create that database with `POST /tenants` after addressing the reported issue. Poll status again before ingestion.

<div className="api-before-related-resources" />

<Tip>
  **Related Resources**

  - **Create:** [Create Database](/api-reference/v2/endpoint/create-database)
  - **Inspect:** [Database Status](/api-reference/v2/endpoint/database-status)
  - **Inspect:** [Database Stats](/api-reference/v2/endpoint/database-stats)
  - **Delete:** [Delete Database](/api-reference/v2/endpoint/delete-database)
  - **Read more:** [Concepts → Multi-Database Support](/essentials/v2/multi-tenant)
</Tip>