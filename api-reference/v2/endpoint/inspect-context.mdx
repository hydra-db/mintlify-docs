---
title: "Inspect Context"
---

\> ## Documentation Index

\> Use this file to discover all available pages before exploring further.

# Fetch Content

\> Retrieve the original content of an ingested source.

export const Field = ({name, type, required, recommended}) =\> {

  const label = required ? 'required' : recommended ? 'recommended' : null;

  const typeLabel = typeof type === 'string' ? type : null;

  const ariaParts = \[name, typeLabel && `${typeLabel}`, label\].filter(Boolean);

  return \<span aria-label={ariaParts.join(', ')} className={label ? 'field-wrap has-field-tip' : 'field-wrap'} style={{

    position: 'relative',

    cursor: label ? 'default' : undefined

  }} tabIndex={label ? 0 : undefined}\>

      \<span className="field-name-row"\>

        \<code\>{name}\</code\>

        {required && \<span className="field-req"\> \*\</span\>}

        {recommended && \<span className="field-rec"\> ●\</span\>}

      \</span\>

      {type && \<span className="field-type"\>{type}\</span\>}

      {label && \<span className="field-tip" role="tooltip"\>

          {label}

        \</span\>}

    \</span\>;

};

Specify the `source_id` of the knowledge source or memory you want to retrieve.

\<RequestExample\>

  \`\`\`python Python SDK theme={null}

  file = client.source.fetch(

      source\_id="policy\_main",

      tenant\_id="acme\_corp",

      mode="both",

      expiry\_seconds=3600,

  )

  \`\`\`

  \`\`\`typescript TypeScript SDK theme={null}

  const file = await client.source.fetch({

    source\_id: "policy\_main",

    tenant\_id: "acme\_corp",

    mode: "both",

    expiry\_seconds: 3600,

  });

  \`\`\`

  \`\`\`bash cURL theme={null}

  curl -G '[https://api.hydradb.com/source/fetch](https://api.hydradb.com/source/fetch)' \\

    -H "Authorization: Bearer \<your\_api\_key\>" \\

    -H "API-Version: 2" \\

    --data-urlencode "source\_id=policy\_main" \\

    --data-urlencode "tenant\_id=acme\_corp" \\

    --data-urlencode "mode=both" \\

    --data-urlencode "expiry\_seconds=3600"

  \`\`\`

\</RequestExample\>

## Query parameters

| Name                                                                                    | Description                                                                                               |

| --------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |

| \<Field name="source\_id" type="string" required /\>                                       | ID of the source to fetch.                                                                                |

| \<Field name="tenant\_id" type="string" required /\>                                       | Owning tenant.                                                                                            |

| \<Field name="sub\_tenant\_id" type="string or null" /\>                                    | Sub-tenant scope. If omitted, the default sub-tenant is used. (default`null`)                            |

| \<Field name="mode" type="&#x22;content&#x22; or &#x22;url&#x22; or &#x22;both&#x22;" /\> | What to return. See \[Fetch modes\](#fetch-modes). (default`"content"`)                                    |

| \<Field name="expiry\_seconds" type="integer" /\>                                          | TTL of the presigned URL (when `mode` includes `url`). Range `60 ≤ x ≤ 604800` (7 days). (default`3600`) |

## Fetch modes

| Mode                  | Returns                                                                      | Use when                                                                                                     |

| --------------------- | ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |

| `content` _(default)_ | Parsed text `content` field) or base64 for binary files `content_base64`). | You want to render the parsed text in-app or feed it to another model.                                       |

| `url`                 | Presigned URL `presigned_url`) valid for `expiry_seconds`.                  | You want a client or a service to download the original file directly without proxying through your backend. |

| `both`                | Text **and** presigned URL.                                                  | UI flows that show parsed text inline plus a "Download original" link.                                       |

\<Info\>

  Use `mode=url` when you need the original file. Use `mode=content` when you only need extracted text for display, summarization, or prompting.

\</Info\>

### Mode examples

\<Tabs\>

  \<Tab title="mode=content"\>

    \`\`\`json theme={null}

    {

      "success": true,

      "data": {

        "success": true,

        "source\_id": "policy\_main",

        "content": "Section 1. Authentication policies...",

        "content\_base64": null,

        "presigned\_url": null,

        "content\_type": "application/pdf",

        "size\_bytes": 1842233,

        "message": "File fetched successfully"

      },

      "error": null,

      "meta": {

        "request\_id": "9d13aef4-02f4-4e73-8c62-4c2601d04f9d",

        "latency\_ms": 12.3

      }

    }

    \`\`\`

  \</Tab\>

  \<Tab title="mode=url"\>

    \`\`\`json theme={null}

    {

      "success": true,

      "data": {

        "success": true,

        "source\_id": "policy\_main",

        "content": null,

        "content\_base64": null,

        "presigned\_url": "[https://storage.hydradb.com/.../policy\_main.pdf?X-Amz-](https://storage.hydradb.com/.../policy_main.pdf?X-Amz-)...",

        "content\_type": "application/pdf",

        "size\_bytes": 1842233,

        "message": "File fetched successfully"

      },

      "error": null,

      "meta": {

        "request\_id": "9d13aef4-02f4-4e73-8c62-4c2601d04f9d",

        "latency\_ms": 12.3

      }

    }

    \`\`\`

  \</Tab\>

  \<Tab title="mode=both (binary file)"\>

    \`\`\`json theme={null}

    {

      "success": true,

      "data": {

        "success": true,

        "source\_id": "diagram\_png",

        "content": null,

        "content\_base64": "iVBORw0KGgoAAAANSUhEUgAA...",

        "presigned\_url": "[https://storage.hydradb.com/.../diagram.png?X-Amz-](https://storage.hydradb.com/.../diagram.png?X-Amz-)...",

        "content\_type": "image/png",

        "size\_bytes": 92844,

        "message": "File fetched successfully"

      },

      "error": null,

      "meta": {

        "request\_id": "9d13aef4-02f4-4e73-8c62-4c2601d04f9d",

        "latency\_ms": 12.3

      }

    }

    \`\`\`

  \</Tab\>

  \<Tab title="Memory source"\>

    \`\`\`json theme={null}

    {

      "success": true,

      "data": {

        "success": true,

        "source\_id": "mem\_user\_alex\_tone",

        "content": "Prefers concise answers and dark mode.",

        "content\_base64": null,

        "presigned\_url": null,

        "content\_type": "text/plain",

        "size\_bytes": null,

        "message": "Memory fetched successfully"

      },

      "error": null,

      "meta": {

        "request\_id": "9d13aef4-02f4-4e73-8c62-4c2601d04f9d",

        "latency\_ms": 12.3

      }

    }

    \`\`\`

  \</Tab\>

\</Tabs\>

\<ResponseExample\>

  \`\`\`json Success theme={null}

  {

    "success": true,

    "data": {

      "success": true,

      "source\_id": "policy\_main",

      "content": "Section 1. Authentication policies...",

      "content\_base64": null,

      "presigned\_url": "[https://storage.hydradb.com/.../policy\_main.pdf?X-Amz-](https://storage.hydradb.com/.../policy_main.pdf?X-Amz-)...",

      "content\_type": "application/pdf",

      "size\_bytes": 1842233,

      "message": "File fetched successfully"

    },

    "error": null,

    "meta": {

      "request\_id": "9d13aef4-02f4-4e73-8c62-4c2601d04f9d",

      "latency\_ms": 12.3

    }

  }

  \`\`\`

  \`\`\`json Failure theme={null}

  {

    "success": false,

    "data": null,

    "error": {

      "code": "SOURCE\_NOT\_FOUND",

      "message": "Source not found"

    },

    "meta": {

      "request\_id": "9d13aef4-02f4-4e73-8c62-4c2601d04f9d",

      "latency\_ms": 4.8

    }

  }

  \`\`\`

\</ResponseExample\>

## Behavior notes

\<Info\>

  **Text vs binary handling.** When the source is text-parseable (PDF, DOCX, MD, TXT), `content` is populated with parsed text. For non-text/binary files, HydraDB returns `content_base64`. Check both fields when handling unknown content types.

\</Info\>

\* **Recently ingested sources:** Fetching immediately after ingestion may return a record before the parsed text is ready. For reliable reads, poll \[Source Status\](/api-reference/v2/endpoint/source-status) for the `source_id` first.

\* **Presigned URL TTL:** The URL is valid only for `expiry_seconds`. Anyone with the URL can download the file during that window, so treat it as a short-lived secret.

\* **Memory items:** Fetching a memory's `source_id` returns its raw text content. There are no presigned URLs for memory items  -  `mode=url` and `mode=both` return `presigned_url: null`.

## Errors

Common codes: `400 INVALID_PARAMETERS`, `404 SOURCE_NOT_FOUND`, `422 VALIDATION_ERROR`. See \[Error Responses\](/api-reference/v2/error-responses) for the full list.

\<div className="api-before-related-resources" /\>

\<Tip\>

  **Related Resources**

  \* **Discover IDs:** \[List Documents\](/api-reference/v2/endpoint/list-documents)

  \* **Perform a query:** \[Query\](/api-reference/v2/endpoint/query)

  \* **Status check:** \[Source Status\](/api-reference/v2/endpoint/source-status)

  \* **Cleanup:** \[Delete Source\](/api-reference/v2/endpoint/delete-source)

  \* **Read more:** \[Sources - Overview\](/api-reference/v2/endpoint/sources-overview)

\</Tip\>

## OpenAPI

\`\`\`\`yaml api-reference/v2/openapi.json GET /source/fetch

openapi: 3.1.0

info:

  title: Cortex SDK API

  description: REST APIs for Cortex AI retrieval engine

  version: 0.0.1

servers:

  - url: [https://api.hydradb.com](https://api.hydradb.com)

    description: Production

    x-fern-server-name: hydrad-db

security: \[\]

paths:

  /source/fetch:

    get:

      tags:

        - Source

      summary: Fetch document content

      description: Retrieve the content of a previously ingested document or memory.

      operationId: fetch\_source\_fetch\_get

      parameters:

        - name: source\_id

          in: query

          required: true

          schema:

            type: string

            description: Source ID of the document to fetch

            title: Source Id

          description: Source ID of the document to fetch

        - name: tenant\_id

          in: query

          required: true

          schema:

            type: string

            title: Tenant Id

            description: Unique identifier for the tenant/organization

          description: Unique identifier for the tenant/organization

        - name: sub\_tenant\_id

          in: query

          required: false

          schema:

            anyOf:

              - type: string

              - type: 'null'

            title: Sub Tenant Id

            description: \>-

              Optional sub-tenant identifier used to organize data within a

              tenant. If omitted, the default sub-tenant created during tenant

              setup will be used.

          description: \>-

            Optional sub-tenant identifier used to organize data within a

            tenant. If omitted, the default sub-tenant created during tenant

            setup will be used.

        - name: mode

          in: query

          required: false

          schema:

            type: string

            description: 'Fetch mode: ''content'', ''url'', or ''both'''

            default: content

            title: Mode

          description: 'Fetch mode: ''content'', ''url'', or ''both'''

        - name: expiry\_seconds

          in: query

          required: false

          schema:

            type: integer

            maximum: 604800

            minimum: 60

            description: Presigned URL expiry (seconds)

            default: 3600

            title: Expiry Seconds

          description: Presigned URL expiry (seconds)

      responses:

        '200':

          description: Successful Response

          content:

            application/json:

              schema:

                \$ref: '#/components/schemas/SourceFetchApiResponse'

        '400':

          description: Bad Request - Invalid input parameters

          content:

            application/json:

              schema:

                \$ref: '#/components/schemas/ErrorApiResponse'

        '401':

          description: Unauthorized - Authentication required

          content:

            application/json:

              schema:

                \$ref: '#/components/schemas/ErrorApiResponse'

        '403':

          description: Forbidden - Access denied

          content:

            application/json:

              schema:

                \$ref: '#/components/schemas/ErrorApiResponse'

        '404':

          description: Not Found - Resource does not exist

          content:

            application/json:

              schema:

                \$ref: '#/components/schemas/ErrorApiResponse'

        '422':

          description: Unprocessable Entity - Validation failed

          content:

            application/json:

              schema:

                \$ref: '#/components/schemas/ErrorApiResponse'

        '500':

          description: Internal Server Error

          content:

            application/json:

              schema:

                \$ref: '#/components/schemas/ErrorApiResponse'

        '503':

          description: Service Unavailable

          content:

            application/json:

              schema:

                \$ref: '#/components/schemas/ErrorApiResponse'

      security:

        - HTTPBearer: \[\]

components:

  schemas:

    SourceFetchApiResponse:

      properties:

        success:

          type: boolean

          title: Success

          default: true

        data:

          anyOf:

            - \$ref: '#/components/schemas/SourceFetchResponse'

            - type: 'null'

        error:

          anyOf:

            - \$ref: '#/components/schemas/ErrorDetail'

            - type: 'null'

        meta:

          \$ref: '#/components/schemas/ResponseMeta'

      type: object

      title: SourceFetchApiResponse

    ErrorApiResponse:

      properties:

        success:

          type: boolean

          title: Success

          default: true

        data:

          title: Data

        error:

          anyOf:

            - \$ref: '#/components/schemas/ErrorDetail'

            - type: 'null'

        meta:

          \$ref: '#/components/schemas/ResponseMeta'

      type: object

      title: ErrorApiResponse

      description: Standard v2 error response envelope.

    SourceFetchResponse:

      properties:

        success:

          type: boolean

          title: Success

          description: Whether the fetch was successful

          default: true

        source\_id:

          type: string

          title: Source Id

          description: Source ID of the fetched file

        content:

          anyOf:

            - type: string

            - type: 'null'

          title: Content

          description: File content as string (if mode includes 'content')

        content\_base64:

          anyOf:

            - type: string

            - type: 'null'

          title: Content Base64

          description: \>-

            File content as base64 encoded string (for binary files, if mode

            includes 'content')

        presigned\_url:

          anyOf:

            - type: string

            - type: 'null'

          title: Presigned Url

          description: Presigned URL to access the file (if mode includes 'url')

        content\_type:

          anyOf:

            - type: string

            - type: 'null'

          title: Content Type

          description: Content type of the file

        size\_bytes:

          anyOf:

            - type: integer

            - type: 'null'

          title: Size Bytes

          description: Size of the file in bytes

        message:

          type: string

          title: Message

          description: Response message

          default: File fetched successfully

        error:

          anyOf:

            - type: string

            - type: 'null'

          title: Error

          description: Error message if fetch failed

      type: object

      required:

        - source\_id

      title: SourceFetchResponse

      description: Response for file fetch request.

    ErrorDetail:

      properties:

        code:

          type: string

          title: Code

        message:

          type: string

          title: Message

      type: object

      required:

        - code

        - message

      title: ErrorDetail

    ResponseMeta:

      properties:

        request\_id:

          type: string

          title: Request Id

        latency\_ms:

          anyOf:

            - type: number

            - type: 'null'

          title: Latency Ms

      type: object

      title: ResponseMeta

  securitySchemes:

    HTTPBearer:

      type: http

      scheme: bearer

\`\`\`\`